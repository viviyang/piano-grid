import { SUPPLEMENT_REFERENCES } from '@/lib/chord-completion-content';
import './category.css';
export function SupplementIndex({route}:{route:string}){const items=SUPPLEMENT_REFERENCES.filter(item=>item.destination.split('#')[0]===route);if(!items.length)return null;return <section className="am-content-section ch-supplement" id="supplement-references" aria-labelledby="supplement-references-heading"><h2 id="supplement-references-heading">Additional supported references</h2><div className="am-content-body"><p>These compact references add verified formulas and example voicings without a separate page.</p><div className="ch-supplement-grid">{items.map(item=><article id={item.destination.split('#')[1]} key={item.id}><h3>{item.symbol}</h3><p>{item.name}</p><dl><div><dt>Formula</dt><dd>{item.definition.displayFormula.join('–')}</dd></div><div><dt>Written tones</dt><dd>{item.definition.toneSpellings.join(' · ')}</dd></div><div><dt>Example</dt><dd>{(item.realizations.find(realization=>realization.id===item.defaultRealizationId)||item.realizations[0]).notes.join(' · ')}</dd></div></dl></article>)}</div></div></section>}
