# PianoGrid：P0 审核补充与 Codex 完整执行指令

日期：2026-09-22  
建议范围：P0-0 → P0-1 → P0-2，结束后交付人工验收，不自动进入 P1。  
审计代码基线：`c1a017a533289616b84e354c97f1345114f809e2`

## 使用方法

将本文件放入已有审计目录 `docs/product-system-audit-2026-09-22/`，在能访问 `piano-main-product-audit` 的新 Codex 实施会话中要求执行本文件。原审计会话停止写入该工作区；原 `piano` / `codex/chords-b1` 工作区不动。

原 00–09、README、ROUTE_TEMPLATE_INVENTORY、EVIDENCE_INDEX 与原始 evidence/ 保留；不要用本文件覆盖原审计证据。下文是本轮范围收窄、执行补充与指令，不是新的仓库验收报告。只有用户将本指令交给实施会话后，才构成本轮所列本地修改授权；不包含提交、推送、合并或部署。

## 审核依据与边界

[已核实] 本次上传的路由表具有 206 条互不重复的 URL 行。00/README 记录 integration-data 为 126 通过、26 失败，其中 23 项被审计归类为换行差异，3 项为实际 Git 内容变化。证据来源：`ROUTE_TEMPLATE_INVENTORY.md`、`00_CURRENT_STATE.md` 的“本次验证”、README。

[已核实] 07 已限定九页试点；08 明确区分实现、浏览器测试、人工测试与发布。07 将 P1-1 依赖写为“先有 P1-2 接收器”，同时 P1-2 又引用 P1-1 关系数据，需在未来实施前消除依赖歧义。证据来源：`07_IMPLEMENTATION_PLAN.md` 的 P1-1/P1-2，`08_ACCEPTANCE_CRITERIA.md`。

本轮审阅依据是上传文档，未取得仓库源码与 evidence/ 原始日志，不能独立确认文档内全部 REPO_VERIFIED/LIVE_VERIFIED 主张。以下要求由实施者在实际工作区复现，不得把本文当作已经复验通过的证据。

[推断] 审计方向可以保留，无需重新做竞品研究或全站产品规划。最小下一步是修通验证基线、两个局部问题及试点测量，再决定 P1 的产品衔接实施。

---

# 给 Codex 的实施指令

这是 PianoGrid 的 Product System **P0 实施会话**。不是研究会话，也不是再次生成一套审计文档。

请依据现有审计包和本文件，在明确授权范围内完成代码、测试、可复现证据与人工验收交接。可自行解决的技术问题请直接处理；不要每完成一个小步骤就等待我回复。遇到权限、受保护来源、基线冲突或必要验证阻塞时，只报告具体阻塞，不伪造通过。

## 1. 本轮做什么，以及不做什么

允许实施：

- P0-0：复现基线、查明失败、修复适用的测试可移植性、跑通本地生产构建浏览器验证。
- P0-1：纠正 Finder 输入帮助；修复 `/guide/piano-chords` 的既有失效链接。
- P0-2：基于已有 analytics 补 Finder 调用、试点有效结果曝光与必要事件映射/去重。

本轮不实施：

- P1-1 九页新下一步呈现、P1-2 跨页状态恢复、P1-3 来源补映射/PDF 内容变更。
- Practice Queue、Embed、账户、MIDI、麦克风、商业化、新 comparison 页面。
- 新音乐引擎、统一所有键盘/音频的重构、新统计 SDK、全站埋点迁移。
- 新增、删除、合并 SEO 页面，修改 canonical/robots/sitemap 策略，批量修改 TDH。
- 导航改版、主题/字体/tokens 改版，回退已批准琴键样式。
- commit、push、merge、deploy；操作生产统计后台或发送测试数据到生产报表。

“READY_FOR_IMPLEMENTATION”是审计可交接状态，不等于所有 P0/P1 项已获得实施授权，也不是发布许可。

## 2. 工作区、资料与版本保护

优先使用已有独立审计工作区：

`C:/Users/Admin/Documents/viviyang_github/piano-main-product-audit`

资料目录：

`docs/product-system-audit-2026-09-22/`

先确认 pwd、Git 仓库根目录、HEAD、branch、git status、worktree list、锁文件 hash。审计资料本身可能未被 Git 跟踪，不能当垃圾文件删除。

- 原 `piano` 工作区及 `codex/chords-b1` 的脏改动不得改写、清理、暂存、搬迁或自动合并。
- 确认没有另一会话同时修改本审计工作区，再在当前已核实审计 HEAD 创建专用实施分支，例如 `codex/product-system-p0`。已有同名分支时先核对，不覆盖。
- 允许 fetch 用于检查远端进展，但不自动 pull/rebase/切换基线。
- 若 main 已前进，对涉及的代码、规则、路由和数据检查做局部差异复核。若变化无关，可以保留本次审计 SHA 开展本地实现，同时记录落后范围；若相关，先给出冲突定位及安全对齐方案，不能凭“最新”二字覆盖已批准实现。
- 实施基线一旦确认，本轮冻结，不在中途追着 main 变化。
- 如当前宿主必须建立新 worktree，先将整个审计资料目录及必要规则快照复制过去并校验文件清单/hash；不要只拉 main 后假定这些未提交材料自动存在。
- 缺资料时先按 EVIDENCE_INDEX 在已知审计工作区查找。不能凭报告中的文件名重造原始日志，也不能声称已经读取缺失文件。
- 不读取或输出不必要的秘密；不要把 .env、Cookie、登录凭据放入交付包。

阅读顺序：

1. 当前环境与仓库适用的 AGENTS.md / 上级规则。
2. 本文件。
3. 审计 README、00_CURRENT_STATE、06_P0_P1_BACKLOG、07_IMPLEMENTATION_PLAN、08_ACCEPTANCE_CRITERIA、09_DECISION_LOG。
4. EVIDENCE_INDEX 及与 P0 有关的原始证据。
5. 当前设计/组件/TDH 规则。
6. 02、03、04、05、ROUTE_TEMPLATE_INVENTORY 在查任务与范围时按需定位；01 用于避免重启已排除的研究假设。

本轮不要求重新阅读三份 Deep Research PDF，不重新核查竞品增长数字。

### 规则与代码事实分开

本基线据审计缺少 `docs/design/COMPONENT-RULES.md`，但包含交接快照 `evidence/COMPONENT-RULES.workspace.md`。先验证该快照真实存在，标明来源；不将其冒充 main 已提交文件，也不忽略其中用户既定的设计约束。

保留现有品牌配色、字体、design tokens、Foundation 和当前琴键表现。通用控件复用实际已有 shadcn/ui 或项目封装；不能假设仓库拥有整套 shadcn Tabs/Slider/Menu。P0 不需要新控件套装，不安装新依赖，不执行 UI 初始化覆盖命令。`npm ci` 仅按现有锁文件恢复依赖。

代码证明实现；生产 HTTP/浏览器证明上线行为；第一方统计证明被测用户行为；用户规则决定本轮允许改什么。不同证据不能互相替代。

## 3. P0-0：把“失败分类”变成可复现结论

保留原始审计结果，所有本轮输出写入新的 checks 目录，不覆盖旧报告。

先运行 00/08 指定的现有 check、integration-data、scale contract、build。记录实际命令、环境、端口、构建目录、退出码和告警。不要重复运行没有服务的默认端口。

### 23 项换行差异

先重新证实每项确实只有所述文本换行差异；不得仅因为报告写了 CRLF 就放行。

如修改 hash 检查的跨平台读取方式：

- 只对明确列入范围的文本文件处理 CRLF/LF 差异；二进制资源继续逐字节校验。
- 保留原始 hash、受保护基准、Git 对象内容和当前工作树内容的对应关系。
- 不能只读取 `git show HEAD:path` 而漏掉当前工作树中尚未提交的实质修改。
- 不得通过 trim、重排 JSON、删除字段、移除空白等宽泛归一化掩盖内容变化。
- 测试至少证明：合法换行差异可以识别；当前工作树中仅改一个实质字符，仍会失败。
- 不批量重写被保护数据，不为过测试修改全仓换行设置。

### 3 项实际内容变化

审计将其描述为 A-Scales batch-page-content、batch-source-ledger、source-ledger.master。请从原始 hash-diagnosis 与源码取得实际完整路径，逐项记录：

`path / old_expected_hash / baseline_hash / working_tree_hash / introducing_commit / actual_diff / affected_scope / approval_reference / disposition`

“Git 中已经提交”不等于“符合原始受保护内容的变更授权”。

- 已有明确批准记录且覆盖该具体变更时，可据原合同做精确、可追溯的基线维护；保留旧值、批准引用与变更理由。
- 找不到批准记录时，保留失败与阻塞，列出需要用户确认的具体文件/差异；不要自动把 expected hash 改成当前值。
- 不把三个真实变化重新归类为换行，也不覆盖来源台账来取悦检查器。
- 当前工作树若出现第四项实质差异，必须单列，不能纳入“已知 26 项”。

P0-0 的必要门禁实际通过，或已有用户明确认可且范围适用的例外后，再进入依赖它的产品修改。没有授权例外就不自行创造例外。若阻塞尚未解决，可交付已完成的诊断/测试修复，但状态保留 BLOCKED，不宣布整个 P0 完成。

### 浏览器验证

使用实际 production build 启动本地服务；读取已有浏览器脚本支持的 base URL/端口配置后运行。采用当前环境已经授权、可用的浏览器能力；不要求特定工具名字，但必须真实执行对应步骤。不得为了绕过权限或环境限制换途径。

先留下九页桌面/移动视口基线与关键交互证据。移动视口不等于真机，HTML 抓取不等于交互测试，音频调度断言不等于真人听音。

同一原因失败两次后，先定位和报告原因，不继续盲重试。浏览器无法运行时可完成可执行的诊断工作，但浏览器门禁保持 BLOCKED/NOT_RUN。

## 4. P0-1：只修已定位的两处问题

### Finder 帮助

沿 07 指定的可维护发布 adapter/实际调用链定位，检查渲染后的帮助，不改受保护原始内容包。

英文帮助必须与本次已验证界面一致：选择音名/pitch classes，可选 bass；不要求用户寻找不存在的带八度文本输入框，不暗示能恢复原始 octave 或重复音，不声称支持麦克风/MIDI。保留正确的 root/bass 与多候选解释，不新增 parser、不改 matcher。

文案采用普通用户能看懂的表达，不把内部数据合同术语搬到主操作提示里。

### Guide 链接

按 07 定位 `/guide/piano-chords` 中指向 `/chords#find-a-chord` 的链接，修到已有 `/chords/finder`，并确认锚文本和上下文确实描述“由音找和弦”的任务；若原文描述的是按名称浏览，记录具体冲突，不盲目改成不同任务入口。

回归断言：旧误导说明不再出现在相关输出；guide 进入正确页面；受保护资料 hash 没变；其他路由正文和 metadata 不受全局字符串替换影响。

## 5. P0-2：复用事件前，先证明语义一致

不要重装 GA4、GTM、Clarity，不建第二套 transport，不重构全站 analytics。

九页新增测量范围：

```text
/chords
/chords/finder
/chords/c-major
/chords/a-minor
/chords/c-maj7
/chords/c-diminished
/chords/c-add9
/scales/c-major
/keyboard-notes
```

公共模块可以做必要兼容修改，但新增触发/新测量行为应限于本轮范围；既有非试点事件保持兼容，逐模板回归。不因需要测量就先实现 P1 CTA。

先根据真实源码形成事件映射，字段至少包括：

`concept / existing_wire_event / actual_trigger / payload / scope / dedupe_key / numerator / denominator / version_change`

这个映射是实施细节，可直接随代码交付，不需要让我再批准一份空泛方案。

### 5.1 不按事件名猜测语义

07 提议将 `scale_reference_viewed` 复用于 `result_seen`。必须先检查它何时触发。

- 若它代表页面加载或组件挂载，不能直接等同于“答案已经进入可见区域”。
- 若需要新的可见结果事件，保留旧事件用途，通过不同概念/版本映射，不能让旧新两种记录被同一报表重复累计。
- 同一个业务概念只能有一个权威计数来源；不同事实事件可以共存，但不能相加冒充额外的任务次数。
- 不能为统一命名删除旧事件，造成历史报表断层；有必要语义变化时记录版本与不可直接比较的区间。

### 5.2 结果曝光、点击和完成分别定义

- Detail 的 `result_seen`：当前有效答案块真实可见后记录；按 logical view × 对象/必要结果状态去重。不要把 rerender、重新滚入视口、监听器重复安装算新曝光。
- Finder 的 `tool_start`：首次有效用户输入；页面默认值和程序恢复状态不冒充用户开始。
- Finder 的输入 fingerprint 只使用当前模型真实支持的 pitch classes、bass、过滤模式；不添加不存在的 octave 输入维度。
- “计算出候选”与“用户看到候选”分开。多候选列表默认是一组结果，不把所有候选行自动膨胀成多个独立完成任务。
- 输入稳定的规则必须明文定义并有确定性测试；异步旧结果不能覆盖新结果或为新状态发事件。
- 空/单音遵守 07 的 incomplete 口径，不发正式 no-match；真实 no-match fixture 从当前 registry 取得，不修改 matcher 来制造测试。
- Hub 无单一对象时，不把页面上所有默认卡片都当用户已查到答案；为可解释的任务区域明确分母。
- 下一步点击是 intent，不是目标页任务成功；播放结束不是真琴演奏成功。
- 提示/揭晓后的练习标 assisted；自报完成标 self_reported；退出不计成功。
- 打印请求不等于纸张已打印，下载点击不等于保存成功；分享区分打开、复制成功、系统分享成功和取消。
- 音频“成功开始”应取已有成功回调/状态；仅有按钮点击证据时只记 request，不重写音频引擎来掩盖证据不足。

### 5.3 报表分母必须一致

如定义“下一步参与率”，采用：

```text
至少发生一次合格下一步动作的、去重后的有效结果曝光数
÷
同一范围/版本/观察口径下的有效结果曝光数
```

不能用可重复点击的 raw event_count 除以不重复的 result_seen，随后把超过 100% 的结果称为用户转化率。

音乐练习按 mode/outcome/assisted 分开，不直接横比不同练习的“完成率”。保留零分母/样本不足为 NO_DATA，不写 0% 或判失败。

`result_seen` 只是看到了结果，不是已经理解或完成任务。用户查到答案后离开可能已经满足需求：本轮不能自动把“没有下一步”命名为任务失败，也不能为提高点击率强制用户进入练习。

### 5.4 生命周期、隐私与测试

- 去重键优先保存在当前页面/任务生命周期内；不为埋点增加跨站身份识别或新的持久用户标识。
- 固定枚举与现有对象 ID；不上传任意输入、个人信息、完整未经清理的 URL/query。
- 遵守已有同意/停用机制；不得强制开启 SDK、绕过权限或补发用户未同意期间的行为。
- transport 缺失、被禁用或抛错时，查找、播放和练习仍可正常工作。
- 本地验证使用 stub/测试接收器；不能将回归脚本流量发往生产 GA4 或 Clarity。DebugView 必须是已有授权的测试路径；无权限就保持 BACKEND_NOT_VERIFIED。

自动测试至少覆盖：可见前不发曝光；可见后一次；rerender/重复监听不双发；明确的新 logical view 按既定规则处理；同一状态重复不双计；状态变化后 ID 正确；空态/单音/无匹配；重复点击与去重报表；失败音频不记成功；assisted 不记独立成功；取消分享不计成功；transport 故障不影响 UI；非试点不出现新的统计副作用。

## 6. 明确 P0 验收范围，不假装完成整个 08

08 是完整试点验收合同，本轮只实现 P0，不能为了跑通 P1 验收步骤而越界实现新能力。

在交付 RESULT 内建立范围表：

| 范围 | 本轮要求 |
|---|---|
| P0 必须执行 | 基线失败处理、两处修复、九页测量合同、真实浏览器步骤、相关音乐/SEO/build 回归 |
| 既有行为回归 | 原 reference/practice、音频、现有链接、非试点模板、现有打印/分享不被破坏 |
| P1 尚未实施 | 新 Next Step 呈现、跨页状态恢复、来源补映射、打印资产内容完善；明确 DEFERRED，不计 PASS |
| 外部/人工待验 | 真机、读屏、真人听音/音乐审阅、真实纸上打印、GA4 后台；按本次变更是否适用列实际状态，未执行不代签 |
| 发布权限 | 本轮没有；无论本地检查结果如何均不发布 |

这是阶段区分，不是删除 08 的发布门禁。

P0 未改音乐事实/来源/声部/PDF 时，不要求先完成整个 P1 的具名来源审阅才开始 P0 编码；但仍需相关音乐与音频回归。若发现必须修改音乐事实才能完成 P0，超出本轮范围，停止该改动并报告。

没有后台权限，不阻止本地测量代码与浏览器测试；它阻止声称后台收数已经验证。没有足够流量，不阻止修复已证实的误导文案与断链；不能把“等待 28/90 天数据”加成 P0 开始条件。

适用的本地必需检查未通过时，不得使用 READY_FOR_MANUAL_ACCEPTANCE。基线历史失败只有明确适用的用户认可例外才能单列豁免，不能由实施模型自行降级。

## 7. P1 与 Page Gate 的两处澄清：只记决策，不实施

### P1 依赖改为无循环顺序

将未来执行顺序明确为：

```text
明确最小 object/context/state 协议与 fixture
→ P1-2 接收端及其恢复测试
→ P1-1 页面链接/下一动作呈现
→ P1-3 限定来源和已有资产
```

协议/关系引用可独立定义，不要求先上线 P1-1 UI。不要让 P1-1 和 P1-2 互相等待。

P0 完成后，P1 仍需下一次范围确认；不自动开始，也不要求必须等待数周 SEO 数据才允许提出 P1 实施。

### Page Gate 不等于每页必须发明新功能

保留 05 的六项规则，但明确 Unique Utility 指“对当前独立任务的价值”，不是“该 URL 独占一种控件”。同一键盘和音频引擎可以服务不同的明确对象查询。

“有下一步入口”不代表用户必须点击。独立 URL 判断仍需任务/答案/需求/来源证据；既不因共用模板否定全部和弦详情，也不因 registry 有对象就自动建页。本轮不改任何已有页的索引决策。

把这两处澄清写在本轮 RESULT 的决策附注即可，不重写整套 00–09。

## 8. 交付内容与最终状态

集中输出到：

`docs/product-system-p0-2026-09-22/RESULT.md`

原始证据放：

`checks/product-system-p0-2026-09-22/`

RESULT 至少包含：

1. scope=P0；执行 HEAD/分支/工作目录；相对远端 main 的差异边界。
2. P0-0/P0-1/P0-2：实现状态、验证状态、证据路径分列。
3. 26 项失败的逐项处理表；3 项真实变化的路径、diff、提交与批准出处；新增失败单列。
4. 事件实际映射表、可见性与去重规则、报表分母、本地与后台验证状态。
5. 前后浏览器证据：Finder 帮助、guide 跳转、九页必要流程与非试点回归。
6. 公共代码影响范围、路由集合和 metadata 差异；确认无 P1 提前实施。
7. 变更文件清单、可审查 diff、所有新增未跟踪实现/测试文件清单；不能仅给漏掉新文件的 git diff。
8. 原始检查退出码与失败/警告；适用例外写明认可者、日期与范围。
9. 人工验收步骤：每项写入口、动作、预期、是否需要真机/后台/专业审阅，用户不需要自己从日志设计测试。
10. 下一批 P1 进入条件；本次未提交、推送、合并或部署。

不创建几十份重复总结，不把“文档已生成”写成“产品已验收”。

最终只用以下两个总状态之一，并始终附 scope=P0：

- `READY_FOR_MANUAL_ACCEPTANCE`：本轮 P0 已实施，适用的本地/浏览器自动门禁通过；人工与外部待验项完整列明；不是发布通过。
- `BLOCKED`：基线授权、相关代码冲突、实现或必需本地验证未解决；保留已完成工作，说明最小阻塞和必要下一动作。

请开始执行。先确认资料和工作区，然后推进 P0-0；只有实际前置门禁成立才推进 P0-1/P0-2。不要停在计划或审计摘要，也不要扩大到 P1。
