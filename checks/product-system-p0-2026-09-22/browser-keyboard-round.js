const results=[];await page.goto('http://127.0.0.1:4349/keyboard-notes?practice=v1&practice-option=natural-c4-c5&practice-seed=20260916&practice-count=10#note-trainer',{waitUntil:'domcontentloaded'});await page.locator('.kn-v2-shell[data-hydrated="true"]').waitFor();await page.getByRole('button',{name:'Start 10-note practice',exact:true}).click();
for(let i=1;i<=10;i++){const name=(await page.locator('.kn-v2-task h2').innerText()).replace(/^Find\s+/,'').trim();await page.locator('.kn-v2-question-keyboard').getByRole('button',{name,exact:true}).click();await expect(page.locator('.kn-v2-feedback[data-kind="correct"]')).toHaveCount(1);await page.getByRole('button',{name:/Next note|See results/,exact:true}).click();}
await expect(page.getByRole('heading',{name:'Your round, at a glance.',exact:true})).toBeVisible();const score=await page.locator('.kn-v2-score-track').getAttribute('aria-label');results.push({name:'existing ten-note keyboard round',passed:score==='10 first try, 0 with help, 0 revealed',score});const path=artifactPath('p0-keyboard-round.json');await(await import('node:fs/promises')).writeFile(path,JSON.stringify(results,null,2));return {path,results};
