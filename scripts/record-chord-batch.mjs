/** Derive progress from source registry and actual local reports. No source content edits. */
import fs from 'node:fs';import crypto from 'node:crypto';
const read=p=>JSON.parse(fs.readFileSync(p,'utf8').replace(/^\uFEFF/,'')),write=(p,v)=>fs.writeFileSync(p,v+'\n');
const plan=read('docs/product/url-plan.final.json'),master=read('docs/content/site-master/page-content.master.json'),batches=read('docs/tasks/batch-implementation/batch-scope.json').batches;
const active=['/chords','/chords/a-minor','/chords/a-major','/chords/c-major'];
const registry=read('docs/content/content-source-map.json');
for(const p of registry.pages){p.implementation_status=active.includes(p.url)?'implementation_checked':p.priority==='暂不做'?'blocked_reserved':'queued';p.review_status=active.includes(p.url)?'independent_review_pending':'not_run';p.published=false;}
write('docs/content/content-source-map.json',JSON.stringify(registry,null,2));
const scopes={P148:'implemented: 默认九个图表、筛选、声音、当前/集合打印与读图',P154:'implemented for supplied collection: 两个 minor 命名结果；不声称全量',P158:'implemented for supplied collection: 七个 major 命名结果；不声称全量',P168:'planned / withheld: power 无数据，不显示假选项',P170:'planned / withheld: 7th 类别和结果未准备，未以 major7 替代',P171:'planned / withheld: jazz 未准备，不显示假风格按钮',P238:'implemented: A 与 C 两组大小三和弦具体图、音名表与原解释',P247:'partial: 完整读图；深入指南未授权，链接不开放',P149:'implemented: A major 三位置、音程、齐奏/逐音、打印/PDF；指法 null，不开放未上线相关目标',P163:'implemented: C major 三位置；已提供 RH 原位常见指法例独立说明，其余 null'};
const coverage=active.flatMap(url=>plan.pages.find(p=>p.url===url).source_groups.map(g=>({url,id:g.id,required_output:g.required_output,status:scopes[g.id]||(url==='/chords/a-minor'?'prior approved scope preserved; full regression passed':'pending'),release_gate:'original professional/material/release conditions unchanged'})));
write('checks/batches/01-chords/coverage.json',JSON.stringify(coverage,null,2));
const rows=plan.pages.map(p=>{const batch=p.url==='/chords/a-minor'?'01 regression':batches.find(b=>b.pages.some(x=>x.url===p.url))?.batch_id||'not in 00–07';const status=active.includes(p.url)?'implementation_checked / review pending':p.priority==='暂不做'?'blocked_reserved':'queued / 未授权';return `| ${p.url} | ${p.template_id} | ${p.priority} | ${batch} | ${p.source_groups.map(g=>g.id).join(', ')||'—'} | ${status} |`;});
write('docs/tasks/site-implementation-plan.md',`# 全站实现进度\n\n日期：2026-09-09。唯一当前批次：01-chords，状态 implementation_checked，等待独立验收；不是 review_passed。00 必要接入完成。授权为本轮用户指定的 00+01 顺序及组件模板补充；未授权 02–07。\n\n[已核实] 全站127 URL保持，17先做、91后做、19暂不做。源数据准备状态不变；下表仅工程状态，全部 production published=false。现有 A minor 是前阶段成果，本轮回归单列，不算新建页面。\n\n## 首轮17页\n\n${rows.filter((_,i)=>plan.pages[i].priority==='先做').join('\n')}\n\n## 当前证据与下一步\n\n- 00：checks/batches/00-content-intake/implementation.md；新旧对象和43资产哈希核对、源映射。\n- 01：checks/batches/01-chords/implementation.md；check、逐页测试、A minor 回归、构建与截图。\n- 每页 template、source_groups、来源ID、问题和schema完整登记在 docs/content/content-source-map.json；资源真实路径/哈希/导出状态见 asset-map.json。\n- 本批逐任务组 required_output 与实际覆盖见 checks/batches/01-chords/coverage.json。未做的power/7th/jazz和未上线指南明确保留，不写全部原任务完成。\n- 下一具体动作：用户在独立验收任务中执行 batch-implementation/90-independent-review.md，目标01-chords；开发停止。未自动创建验收任务或运行独立审查。\n\n## 全127 URL保留表\n\n| URL | 模板 | 原优先级 | 工程批次 | source_groups | 实现/测试状态 |\n|---|---|---|---|---|---|\n${rows.join('\n')}\n\n无URL17组与2条历史线索仍在只读master中，未生成路由。未实现页：内容已准备不等于代码或资源已公开；没有默认回退到A minor。`);
const templateRows=plan.page_specs.map(t=>`| ${t.id} | ${t.name} | ${t.user_task} | ${t.id==='T07'?'src/components/chords/detail-page.tsx；A minor、A major、C major，implementation_checked':t.id==='T06'?'src/components/chords/center-page.tsx；/chords，implementation_checked':'planned；无模板代码或路由授权'} |`);
const urlRows=plan.pages.map(p=>`| ${p.url} | ${p.template_id} | ${p.priority} | ${active.includes(p.url)?'implementation_checked / independent review pending':'planned'} | ${p.source_groups.map(g=>g.id).join(', ')||'—'} |`);
write('docs/design/template-map.md',`# 页面任务模板映射\n\n日期：2026-09-09。Source of Truth：docs/product/url-plan.final.json；保留23个原T编号与127 URL。矩阵不授权全站开发，除T06/T07本批实例外均planned。\n\n## 已实现模板与组合\n\n[已核实] T07 ChordDetailPage 在 src/components/chords/detail-page.tsx，被三个很薄的静态路由实际import。A minor继续 getAMinorContent 旧适配；A major/C major使用 getChordDetail 新适配。模板组织固定和弦、原生位置单选、键盘/声/打印、独立原文解释和资料；不是全站任意blocks模板。namespace保存原am块ID，新页无源ID时仅在呈现层生成URL命名空间，原对象不改写。\n\n[已核实] T06 ChordCenterPage 在 src/components/chords/center-page.tsx，仅被 /chords 调用；getChordCenter读取旧对象+legacy_chords_support。默认九结果，筛选/当前对象/发声对象分开；两组大小对比。它共享KeyboardViewport、Keyboard、ReferenceAudio、PlaybackControls、PrintVoicing、SiteHeader/Footer与PageSearch，但不套T07首屏。\n\n[已核实] 服务器负责读取与metadata/正文，客户端仅接收当前工具显示模型。shared code改变后执行：A minor162、三页233、错误降级25、适配14、Foundation558、Tailwind140、cn47及构建；见本批日志，独立验收待执行。所有模板的真人听音、真机、屏幕阅读器、实体打印仍NOT RUN。\n\n## 原23任务模板\n\n| ID | 模板职责 | 原页面任务 | 代码与状态 |\n|---|---|---|---|\n${templateRows.join('\n')}\n\n## URL实例矩阵\n\n每行的完整正文/data/source_groups/issues与两种schema映射见 docs/content/content-source-map.json。原sources及127页内容留在服务器只读源。资源状态见asset-map.json，未授权页不导出资产；本轮三份PDF已等字节导出，A minor下载保持。\n\n| URL | template_id | 原优先级 | 实现/验收 | source_groups |\n|---|---|---|---|---|\n${urlRows.join('\n')}\n\n02琴键(T03/04/05)、03音阶(T11/12)、04歌曲(T15/16)、05指南(T21/22)、06空白谱纸(T19)、07站点整合(T01/02)保留各自任务；T17/18及其他模板仅登记，不提前生成空实现。`);
const snapshot=read('checks/batches/00-content-intake/source-before.json');const after=snapshot.map(x=>({...x,current_sha256:crypto.createHash('sha256').update(fs.readFileSync(x.path)).digest('hex')}));after.forEach(x=>x.unchanged=x.sha256===x.current_sha256);write('checks/batches/01-chords/source-after.json',JSON.stringify(after,null,2));
const progressPath='docs/tasks/site-implementation-plan.md';
write(progressPath,fs.readFileSync(progressPath,'utf8').replace('## 首轮17页\n\n','## 首轮17页\n\n| URL | 模板 | 原优先级 | 工程批次 | source_groups | 实现/测试状态 |\n|---|---|---|---|---|---|\n'));
console.log(`Registry ${registry.pages.length}; templates ${plan.page_specs.length}; required_output records ${coverage.length}; original snapshots unchanged ${after.filter(x=>x.unchanged).length}/${after.length}`);
