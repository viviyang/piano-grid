import { createRequire } from 'node:module';
import { mkdir, writeFile } from 'node:fs/promises';

const {chromium}=createRequire(import.meta.url)(process.env.PIANO_PLAYWRIGHT_PATH||'C:/Users/Admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const base=process.env.PIANO_BASE_URL||'http://127.0.0.1:3001',out=process.env.PIANO_CHECK_OUT||'checks/batches/06-blank-sheet';await mkdir(`${out}/screenshots`,{recursive:true});
const routes=['/chords','/chords/a-minor','/chords/a-major','/chords/c-major','/keyboard-notes','/keyboard-notes/labeled','/keyboard-notes/chart','/scales','/scales/c-major','/scales/a-minor','/songs','/songs/easy','/guide','/guide/read-sheet-music','/tools/blank-sheet-music'];
const integrated=['/','/tools'];
const forbidden=['/sheet-music','/tools/piano-cheat-sheet','/tools/anything'];
const results=[],errors=[];const check=(name,passed,detail='')=>{results.push({name,passed:Boolean(passed),detail});if(!passed)console.error('FAIL',name,detail);};
const browser=await chromium.launch({channel:'chrome',headless:true});const page=await browser.newPage({viewport:{width:1440,height:950}});page.on('pageerror',error=>errors.push(error.message));
try{for(const route of routes){const response=await page.goto(base+route),html=await response.text();check(`${route} production 200`,response.status()===200,response.status());check(`${route} production noindex`,(await page.locator('meta[name=robots]').getAttribute('content')).includes('noindex'));check(`${route} excludes source payload`,!html.includes('source_usage_batches')&&!html.includes('retained_without_url')&&!html.includes('D-sheet-music-reuse'));if(route==='/tools/blank-sheet-music'){await page.waitForFunction(()=>!document.querySelector('.bs-paper-options').disabled);for(const width of [1440,390,320,768]){await page.setViewportSize({width,height:950});await page.waitForTimeout(60);check(`Blank sheet production responsive ${width}`,await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));await page.screenshot({path:`${out}/screenshots/production-blank-sheet-${width}.png`,fullPage:true});}}}for(const asset of ['blank-piano-staff-letter.pdf','blank-piano-staff-a4.pdf','blank-piano-staff-preview.svg'])check(`production asset ${asset}`,(await page.request.get(`${base}/reference/assets/${asset}`)).status()===200);for(const route of integrated)check(`production includes ${route}`,(await page.request.get(base+route)).status()===200);for(const route of forbidden)check(`production excludes ${route}`,(await page.request.get(base+route)).status()===404);check('production has no runtime errors',errors.length===0,errors);}finally{await browser.close();}
const report={executed_at:new Date().toISOString(),passed:results.filter(item=>item.passed).length,failed:results.filter(item=>!item.passed).length,results};await writeFile(`${out}/production-validation.json`,JSON.stringify(report,null,2)+'\n');console.log(`Blank sheet production: ${report.passed} passed, ${report.failed} failed.`);process.exitCode=report.failed?1:0;
