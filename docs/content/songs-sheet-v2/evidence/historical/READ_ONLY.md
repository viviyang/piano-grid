# 历史材料只读副本

这些文件来自可访问的用户资料，不是本轮发布清单。保留原名称、关键词、URL、权利状态、来源日期、占位/暂停状态及历史资产引用，以免缩减任务。

不得把此目录整包复制到网站 public/，不得把历史 asset 路径当本包已生成文件。本包实际交付资产仅在 `content-data/assets/`，由资产清单确认。

已恢复 JSON 的 SHA-256 与原 master 记录完全一致。中文统一规划的选定 Library 副本与 Launch Handoff ZIP 本次读取/物化未成功，因此没有假装检查源码或中文全文；Codex 开始时须在仓库核对当前有效文件与批准变更。不要仅按文件日期覆盖实际已批准结果。

265 处记录按具体来源、版本、曲目归为 200 个版本候选；不等于 200 个可发布乐谱。两处 `Easy Piano` 与 `Easy Piano (collection classification)` 表述变体保存在 conflicts 中；这不是已经证实的音乐冲突，统一展示可明确写“publisher collection label”，不得升级为单曲技术审核。
