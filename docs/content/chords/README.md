# 和弦内容（只读）

[已核实] 当前两个内容文件按原字节放置，版本 `1.0.0-batch-01`。
Foundation 不读取这些文件来生成页面，不修改 URL、词、内容、数据或发布状态。

原内容包提到的 `assets/` 原始二进制未在本次交付内重建；后续业务阶段取得原素材后放在
`docs/content/chords/assets/`，再按发布条件映射到实际服务路径。
不要把旧原型里内置的重制 PDF 冒充内容包的原始资产。
