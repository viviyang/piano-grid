# 内链与导航：按任务连接，按状态开放

[已核实] 图谱中的地址来自已确认原计划与B3注册快照。`published_now`只是快照判断，不是已经部署的证据。

## 先接哪几条

第一批新详情完成后：Hub对应卡片 → 新详情；新详情 → Hub；C major ↔ C minor；A major ↔ A-flat major；E major ↔ B major；C minor ↔ A-flat major。其他连接按JSON的语境位置接入，不把全图塞进每页页脚。

`internal-links.json`保存来源、目标、锚文本、链接理由、位置和开放条件，TSV便于人工看。它是候选/位置计划，不应将所有候选自动重复渲染。

## 正确的开放条件

先验证目标在当前build中存在、内容与功能齐备，再显示链接。当前第一批不因后续5个系统页已经有文案就提前开放它们的导航入口。周边解释保留，不能因为目标未发布就删掉整段原理。

[已核实] B3 `isPublicRoute`使用精确路径判断；`/chords/g-major#practice`不应当成新的路由注册。先分离path/hash，验证path注册和目标anchor存在，再输出真实 `<a href>`。不要依靠按钮onclick代替需要抓取的页面链接。[来源 PG-GOOGLE-LINKS]

[已核实] Hub里由`keyChordItem`生成的对象当前`url:null`。注册G/E/B的详情后，必须为真实存在的对象解析detailURL；不能只改PUBLIC_ROUTES便认为所有Hub内链自动出现。原19个对象顺序、音区、筛选行为不变。

## 导航层次

全站保留Chords栏目；栏目内：查和弦、学基础、按调、进行、反查。只显示当前可用任务。不要把几十个名字塞进主菜单。对象页仍按B3的音符/左右手/转位/练习/打印顺序；本包新增文字放入现有纵向章节，不重做视觉。

根音×类型在Hub筛选，不建立新URL。调性查询归by-key。C major和C-flat major不是同一页；B和C-flat是等音关联，也不互相canonical。双手、转位、打印与练习用状态/锚点，不批量建索引页。

## 跨项目协作

用户可能同时调整Scales。跨到/scales/c-major的链接只引用现有公开页面，不修改其内容、主题、类型或数据；finger-numbers页中涉及的scale指法范围以该页实际核查结果为准。

## 验收

新页至少有Hub或相关教程入口，不形成孤页；目标未完成时不输出假按钮；每个hash能定位到唯一实际ID；相同目标不在同一小块重复出现。不要为了“互链”把每个页面全部相互连接。
