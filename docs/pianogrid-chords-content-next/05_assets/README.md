# 新生成的参考资源

[已核实] 6份PDF与6份SVG由本包的原始voicing数据生成，不是从竞品复制图片。每份文件包括原位、第一转位、第二转位；白键黑点、黑键白点标记实际发声键。标注具体八度及最低音。

前五页接入时复制对应资源，C-flat先保留在内容包。每个公开文件须匹配`adapter-bindings.json`的真实目标路径。SVG是原始矢量图，不作为新配色主题；PDF为黑白打印参考。

这些不是五线谱、不含通用指法、不含录音。US Letter生成；打印到A4时使用适合纸张，不声称本次实机打印已经通过。PDF不宣称带标签无障碍，主页面HTML需要保留完整可访问文本。

三位置的具体音区沿用原包，所以G/B的示例可能高于G/D；它们是三个独立位置例子而非逐次上行练习。C-flat的Cb4=B3关系明确说明，不能把Cb4改作B4。
