# 第二批：已有正文不等于已实现工具

第一批五个详情完成后再使用。不要现在同时开五个并行改动共享数据的会话。

## 输入

`02_content/support.merge.json`：原master格式的数据子集，正确原blocks保留。
`02_content/support.modules.json`：这次补全的英文章节、H1、编辑纠正及实现边界。
`*.preview.md`：正文与49行按调表、9段进行、8个反查用例的可读视图。
`01_planning/`：所有5页的关键词/TDH与条件内链。

## 实施顺序建议

[推断] 先做 `/guide/piano-chords` 和 `/keyboard-notes/finger-numbers`，然后 `/chords/by-key`，再 `/chord-progressions`，最后 `/chords/finder`。这是依赖/复杂度顺序，不是新URL或SEO机会结论。

教程不重复Hub的19卡片；手指页交付清楚的双手数字图/文字，不承诺每首歌完整指法；by-key保留七个调的49个三和弦行，其中减三和弦可作为文本数据，无须强行套T07；进行保留9个4小节数据与准确transport状态；反查实现独立候选匹配词表，不能拿B3 builder当识别器。

## 反查必须解决

有八度就有最低音；空bass字段不能擦掉实际最低音。C4 E4 G4 A4的候选全符号是C6与Am7/C，而非直接Am7。允许多个候选、重复pitch class、确切无匹配；不要把无匹配说成“不存在和弦”。当前准备了八个真实输入输出用例，运行引擎仍为null。

## 旧字段需要的有依据纠正

- 未提供的 `c-major-rh-fingers.svg` 不显示破图，数字图自行依据mapping制作后验收。
- `chord-progressions-c-e-a.pdf`原包只有声明没有文件；本包不继承其visual QA，下载入口必须隐藏到真实资源制作完成。
- 自编进行的rights移除“official/authorized”假授权意味，标明原始规则示例；不复制第三方谱面或录音。
- finger numbers加入C4与指号4的区别；原正确映射与章节保留。

## 开工文本

把本文件交给Codex，补充本次授权的具体URL，例如：

> 使用当前项目与此内容包，现在只完成 `/guide/piano-chords` 和 `/keyboard-notes/finger-numbers`。把已有内容和modules正文合并为完整服务端页面；保持现有主题、URL、正确原内容。按original template责任实现必要的双手标号图、示例、FAQ和真实内链。不得用T07的三音/三转位强制模型套教程。完成后集中做页面与回归验证，输出证据，不提交不部署，不顺手做by-key/进行/finder。

不再完整重做B1/B2/B3。材料缺项只阻断相关功能；不能因此随意删除正确正文或反复导出全包。
