# Chord evidence and engineering round — 2026-09-18

Worktree: `pianogrid-final-integration` at `codex/final-integration` / `6b95908`.
Not committed, not pushed, not deployed.

## Inventory

Unique Chord-module URLs (A) = 158 = 9 family + 145 detail + 4 hub/tools.
The 138 pack is 137 pending details + `/chords/seventh`. It is not a full SEO verdict for all 145 details.
`/chords/g-major` sits in both original-plan mapping and the 138 pending set, which is why 137+9=146 was wrong.

## This round

- Imported query observations without family-wide volume inference.
- Reused and tested the structured-root candidate generator. No remaining B/B-flat `flat major` parse error in current output.
- Kept the Browse two-layer hub. Desktop and mobile discovery checks ran locally.
- Replaced user-facing implementation copy. SSR cards/links/reference data were not removed.
- Did not batch-change detail Title/H1. Seventh family Title is a review candidate only (`approved=false`).

## Not done (first engineering pass)

- No SERP intent verification.
- No index/canonical/robots/URL changes.
- No commit/push/deploy.

## Incremental evidence pass (same day)

- Merged 283 query observations (195 retained + 88 new independent keys). Status now 86 / 47 / 2 / 3 / 0. 133 pages have at least one positive. Not VERIFIED_SEO.
- 04_FULL_CHORDS_158 vs current PUBLIC_ROUTES chord set: empty difference. 206 public routes; live sitemap unique URLs also 206.
- URL_ORIGIN_LEDGER.csv: 158/158 first Git definition commits. N2D first appears in the same commit as N2C (`49e003e`). origin_date is commit time, not production publish.
- Competitor `sitemap.xml` fetched as real XML (820 loc / 803 unique). Unclassified 300 are extra altered/extended/bass pages, not D-minor misreads.
- Core 14 and five no-positive URLs remain review-only. No Title/H1 batch rewrite, no URL change.
- GSC numeric fields left empty. No commit/push/deploy.

## 2026-09-19 B7 query append (C-group TDH alignment, read-only)

- Appended 10 independent Semrush US observations from user screenshot `c39ac412-1136-4fe2-a36f-3220b0047b50.png` (file not in worktree; values transcribed from the prompt).
- Historical `b7 piano` = 590 retained. `b7 chord` / `b7 chord piano` / `b7 chord in piano` are separate queries; volumes are not summed.
- Guitar queries recorded as `GUITAR_EXCLUDED` with empty URL.
- `intent_fit` remains `UNCHECKED`. No Title/H1 applied (`BASELINE_BLOCKED`).
- Round folder: `docs/seo/chords/evidence-2026-09-18/tdh-alignment-2026-09-19/`.

