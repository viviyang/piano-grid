# PianoGrid｜B02/B03/B04 联合修正包

日期：2026-09-16　状态：`AUDIT_COMPLETE / FIX_NOT_IMPLEMENTED`

## 结论

[推断] 保留已经完成的 Explore / Practice 与 B04；在当前 B04 工作树做一轮收口修正。先解决题组、提示、音频等正确性问题，再统一分享、退出、Labeled 页面和 compare 状态。不是重新做 S1，也不是开始 B05/B06。

[已核实] 本包基于两个实际 ZIP、B02/B03 业务源码快照、B04 测试脚本/结果/截图，以及用户本轮 5 张线上截图。两个 ZIP 的 SHA-256 与用户交接消息一致。

[已核实] B04 包没有包含 `src/` 业务源码或完整 diff；B02/B03 包也不是可独立启动的完整仓库。因此本次完成的是材料审查、静态源码审查与隔离函数复现，不是当前整站/真机/生产环境验收。`evidence/probe-results.json` 说明了复现方法与边界。

## 怎么用

把本文件夹放进当前项目 `docs/product-upgrade/2026-09-16/integration-fix/`。

在**完成 B04 的当前 Cursor 会话**发送：

```text
请读取 docs/product-upgrade/2026-09-16/integration-fix/03_CURSOR_PROMPT.md，
按本包对当前 B02/B03/B04 集成结果实施一轮修正。
当前最新仓库优先；附件旧源码仅用于定位，不得覆盖回仓库。
本包列出的界面/交互修正已授权，其他已批准设计保持不动。
修复、联合回归、截图和交接一次完成；不 commit、不 push、不 deploy。
```

## 文件分工

| 文件 | 谁看 | 用途 |
|---|---|---|
| `01_审查结论与证据.md` | 用户 / Cursor | 哪些是真问题、哪些只是未验证 |
| `02_修正规格_界面内容与复用.md` | Cursor | 修改后的布局、状态、组件边界与英文文案 |
| `03_CURSOR_PROMPT.md` | Cursor | 执行顺序、范围、停止条件 |
| `04_验收用例.md`、`acceptance-cases.json` | Cursor / 用户 | 新增验收项；全部初始为 NOT_RUN |
| `evidence/` | Cursor | 来源快照、截图、旧缺陷的函数级复现；禁止当生产补丁导入 |

## 唯一实施基线

[已核实] B04 Intake 记录基线 `c9651ba`；B02/B03 历史包记录更早的 `0eac351… + worktree diff`。用户最新截图已来自线上域名，而旧交接写的是当时未部署。

[推断] 以当前 `git status / git log / 当前文件` 确认实际状态。保留 B04 所有成果；不要重置到任何历史 hash。B04 之前新增的 canonical 保留，本轮新增公开 URL 为 **0**。不得用旧部署说明判断当前生产状态。

## 完成标准

先在当前代码核查本包的历史缺陷是否仍存在；存在则修，已修则补证据。新验收用例、原 B02/B03 回归、B04 回归与相关共享消费者回归完成；人工项未验证就保留 PENDING。最终交接应包含最新业务源码或完整 diff，不能只打包报告和截图。
