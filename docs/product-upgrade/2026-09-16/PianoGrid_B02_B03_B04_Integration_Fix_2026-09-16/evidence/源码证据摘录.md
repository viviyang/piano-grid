# 原始源码证据摘录

[已核实] 以下为附件的原样片段，L 为附件行号；不是修复后的生产代码。

## F01 / F04

来源：`src/components/keyboard-notes/keyboard-notes-workspace.tsx`

```text
L260:   useEffect(() => {
L261:     if (phase !== 'start') return;
L262:     if (sharedPreset.status === 'valid') {
L263:       setOption(sharedPreset.preset.option);
L264:       setRoundPreset(sharedPreset.preset);
L265:     } else if (sharedPreset.status === 'invalid') {
L266:       setRoundPreset(null);
L267:     }
L268:   }, [sharedPreset, phase]);
L269: 
L270:   function resetQuestionState() {
L271:     setWrongMidi(null);
L272:     setAttemptedWrong(false);
L273:     setHinted(false);
L274:     setRevealed(false);
L275:     setResolved(null);
L276:   }
L277: 
L278:   function startPractice(reviewTargets?: PracticeTarget[]) {
L279:     audio.cancel();
L280:     const preset = reviewTargets?.length
L281:       ? roundPreset
L282:       : sharedPreset.status === 'valid' && phase === 'start' && questions.length === 0 && records.length === 0
L283:         ? sharedPreset.preset
L284:         : createPracticePreset(option);
L285:     const next = reviewTargets?.length ? reviewTargets : generatePresetPractice(layout, preset!);
L286:     if (!reviewTargets?.length) setRoundPreset(preset);
L287:     setQuestions(next);
L288:     setIndex(0);
L289:     setRecords([]);
L290:     setIsReview(Boolean(reviewTargets?.length));
L291:     advancing.current = false;
L292:     resetQuestionState();
L293:     setPhase('question');
L294:     emitKeyboardPracticeEvent(reviewTargets?.length ? 'keyboard_practice_review_started' : 'keyboard_practice_started', { option, count: next.length });
L295:   }
```
```text
L328:   function nextQuestion() {
L329:     if (!target || !resolved || advancing.current) return;
L330:     advancing.current = true;
L331:     const nextRecords = [...records, { target, kind: resolved }];
L332:     if (index + 1 >= questions.length) {
L333:       setRecords(nextRecords);
L334:       setPhase('results');
L335:       const summary = summarizePractice(savedResult ?? nextRecords);
L336:       if (!isReview) emitKeyboardPracticeEvent('keyboard_practice_completed', { count: summary.total, first: summary.independent, assisted: summary.assisted, revealed: summary.revealed });
L337:       return;
L338:     }
L339:     setRecords(nextRecords);
L340:     setIndex(value => value + 1);
L341:     resetQuestionState();
L342:   }
L343: 
L344:   function finishReview() {
L345:     setPhase('results');
L346:     setIsReview(false);
L347:     setRecords(savedResult ?? records);
L348:   }
L349: 
L350:   function leavePractice() {
L351:     audio.cancel();
L352:     setPhase('start');
L353:     setQuestions([]);
L354:     setRecords([]);
L355:     setSavedResult(null);
L356:     setRoundPreset(sharedPreset.status === 'valid' ? sharedPreset.preset : null);
L357:     setIsReview(false);
L358:     resetQuestionState();
```
```text
L388:   return <section id="kn-v2-practice-panel" role="tabpanel" aria-labelledby="kn-v2-practice-tab" hidden={!active} className="kn-v2-panel kn-v2-practice" data-phase={phase}>
L389:     {phase === 'start' && <div className="kn-v2-practice-start" id="note-trainer">
L390:       {sharedPreset.status === 'valid' && <div className="kn-v2-receiver-note"><p className="kn-v2-kicker">Practice shared with you</p><p>Start your own round with the same 10-note set. No one else’s answers or score are included.</p></div>}
L391:       {sharedPreset.status === 'invalid' && <div className="kn-v2-receiver-note kn-v2-receiver-invalid" role="alert"><p className="kn-v2-kicker">This practice link is no longer available</p><p>You can still start a new 10-note practice below.</p></div>}
L392:       <div className="kn-v2-start-meta"><span>Piano note practice</span><span>{optionConfig.label.replace('Natural notes · ', '').replace('Black keys · ', '')} · {option === 'black-c4-c5' ? 'Black keys' : 'White keys'}</span></div>
L393:       <div className="kn-v2-start-intro">
L394:         <div className="kn-v2-start-copy">
L395:           <p className="kn-v2-start-note">10-note round · Your pace</p>
L396:           <h2>Find your way around the keys.</h2>
L397:           <p>See a note name, choose its key, then revisit the notes that need another try.</p>
L398:           <button className="am-button am-primary kn-v2-start-button" type="button" onClick={() => startPractice()}><RollingText>Start 10-note practice</RollingText></button>
L399:           <p className="kn-v2-start-summary">{optionConfig.label} · {showLabels ? 'With labels' : 'Without labels'}</p>
L400:         </div>
L401:         <aside className="kn-v2-round-outcome" aria-label="What happens in this practice">
L402:           <p className="kn-v2-kicker">After the round</p>
L403:           <ol>
L404:             <li><strong>See your result</strong><span>First tries, helped answers, and revealed notes stay distinct.</span></li>
L405:             <li><strong>Review missed notes</strong><span>Practice only the notes that need another look.</span></li>
L406:             <li><strong>Share the practice</strong><span>Invite someone to start a fresh round without sharing your answers.</span></li>
L407:           </ol>
L408:         </aside>
L409:       </div>
L410:       <div className="kn-v2-start-keyboard" aria-hidden="true"><KeyboardDiagram keys={keysInRange(layout, 60, 72)} selected={62} fit autoCenter={false} showVisualLabels={false}/></div>
L411:       <div className="kn-v2-start-keyboard-meta"><span>One small part of the piano.</span><span>C4–C5</span></div>
L412:       <details className="kn-v2-options">
L413:         <summary>Practice options</summary>
L414:         <div className="kn-v2-option-fields">
L415:           <label>Notes and range<select value={option} onChange={event => { setOption(event.target.value as PracticeOption); setRoundPreset(null); }}>{Object.entries(PRACTICE_OPTIONS).map(([value, copy]) => <option value={value} key={value}>{copy.label}</option>)}</select></label>
L416:           <label className="kn-v2-checkbox"><input type="checkbox" checked={showLabels} onChange={event => setShowLabels(event.target.checked)}/><span>Show key labels during practice</span></label>
L417:         </div>
L418:       </details>
```
```text
L435:         <KeyboardDiagram
L436:           keys={questionKeys}
L437:           selected={selectedMidi}
L438:           selectedState={selectedState}
L439:           selectedLabel={wrongKey ? `You chose ${primaryLabel(wrongKey)}` : target.label}
L440:           sounding={audio.sounding}
L441:           onSelect={chooseAnswer}
L442:           fit
L443:           autoCenter={false}
L444:           showVisualLabels={showLabels}
L445:           keyLabels={labels}
L446:           isKeyDisabled={key => option === 'black-c4-c5' ? key.color !== 'black' : key.color !== 'white'}
L447:           label={`Question ${index + 1}. Find ${target.label}`}
L448:         />
L449:         <div className="kn-v2-question-keyboard-meta"><span>{showLabels ? 'Note names shown' : 'Note names hidden'} · {optionConfig.label.split(' · ')[1]}</span><span>{option === 'black-c4-c5' ? 'Black keys only' : 'White keys only'}</span></div>
L450:       </div>
L451:       <div className="kn-v2-feedback" aria-live="polite" data-kind={revealed ? 'revealed' : resolved ? 'correct' : wrongMidi !== null ? 'wrong' : hinted ? 'hint' : 'idle'}>
L452:         {wrongMidi === null && !hinted && !resolved && <p>Choose a {option === 'black-c4-c5' ? 'black' : 'white'} key. Take your time.</p>}
L453:         {wrongMidi !== null && wrongKey && <p><strong>Not quite.</strong> You chose {primaryLabel(wrongKey)}. {keyPattern(primaryLabel(wrongKey))}</p>}
L454:         {hinted && !resolved && <p><strong>Hint</strong> Look at the black-key pattern. This question will count as completed with help. {keyPattern(target.label)}</p>}
L455:         {revealed && <p><strong>This is {target.label}.</strong> This answer was revealed and will be added to your review.</p>}
L456:         {resolved && !revealed && <p><strong>{resolved === 'independent' ? 'That’s it.' : 'Found it.'}</strong> {target.label} is the right key.{resolved === 'assisted' ? ' This note will be added to your review.' : ''}</p>}
L457:       </div>
L458:       <div className="kn-v2-question-actions">
L459:         {resolved ? <button className="am-button am-primary" type="button" onClick={nextQuestion}><RollingText>{index + 1 === questions.length ? 'See results' : 'Next note'}</RollingText><Icon name="right"/></button> : <>
L460:           {wrongMidi !== null && <button className="am-button am-primary" type="button" onClick={() => setWrongMidi(null)}><RollingText>Try again</RollingText></button>}
L461:           <button className="am-button am-secondary" type="button" onClick={showHint} disabled={hinted}><RollingText>Hint</RollingText></button>
L462:           <button className="am-button am-tertiary" type="button" onClick={showAnswer}><RollingText>Show answer</RollingText></button>
L463:         </>}
L464:       </div>
L465:       <p className="kn-v2-audio-status" role="status">{audio.message}</p>
L466:       <dialog ref={exitDialog} onClose={() => undefined}>
L467:         <div className="am-dialog-head"><h2>Leave this practice?</h2><button className="am-button am-tertiary" type="button" aria-label="Close" onClick={() => exitDialog.current?.close()}><Icon name="close"/></button></div>
L468:         <p>Your current answers will not be kept.</p>
L469:         <div className="kn-v2-dialog-actions"><button className="am-button am-primary" type="button" onClick={() => { exitDialog.current?.close(); leavePractice(); }}><RollingText>Leave practice</RollingText></button><button className="am-button am-secondary" type="button" onClick={() => exitDialog.current?.close()}><RollingText>Keep practicing</RollingText></button></div>
L470:       </dialog>
```

## F02

来源：`src/components/keyboard-notes/keyboard-notes-workspace.tsx`

```text
L49: function keyPattern(label: string) {
L50:   const letter = label[0]?.toUpperCase();
L51:   if (letter === 'D') return 'D is the white key between the two black keys.';
L52:   if (letter === 'C') return 'C is the white key just before a pair of black keys.';
L53:   if (letter === 'E') return 'E is the white key just after a pair of black keys.';
L54:   if (letter === 'F') return 'F is the white key just before a group of three black keys.';
L55:   if (letter === 'G') return 'G is the white key between the first two keys in a group of three black keys.';
L56:   if (letter === 'A') return 'A is the white key between the last two keys in a group of three black keys.';
L57:   if (letter === 'B') return 'B is the white key just after a group of three black keys.';
L58:   return 'Use the repeating pattern of two and three black keys to find the note.';
```
```text
L451:       <div className="kn-v2-feedback" aria-live="polite" data-kind={revealed ? 'revealed' : resolved ? 'correct' : wrongMidi !== null ? 'wrong' : hinted ? 'hint' : 'idle'}>
L452:         {wrongMidi === null && !hinted && !resolved && <p>Choose a {option === 'black-c4-c5' ? 'black' : 'white'} key. Take your time.</p>}
L453:         {wrongMidi !== null && wrongKey && <p><strong>Not quite.</strong> You chose {primaryLabel(wrongKey)}. {keyPattern(primaryLabel(wrongKey))}</p>}
L454:         {hinted && !resolved && <p><strong>Hint</strong> Look at the black-key pattern. This question will count as completed with help. {keyPattern(target.label)}</p>}
L455:         {revealed && <p><strong>This is {target.label}.</strong> This answer was revealed and will be added to your review.</p>}
L456:         {resolved && !revealed && <p><strong>{resolved === 'independent' ? 'That’s it.' : 'Found it.'}</strong> {target.label} is the right key.{resolved === 'assisted' ? ' This note will be added to your review.' : ''}</p>}
```

## F03 / F05

来源：`src/components/keyboard-notes/keyboard-notes-workspace.tsx`

```text
L135:   function findNote() {
L136:     const resolution = resolveLookup(query, layout);
L137:     if (resolution.status === 'selected' && resolution.selected) {
L138:       setResolution(resolution);
L139:       setQuery(resolution.selected.requestedSpelling.display);
L140:       bringIntoView(resolution.selected.midi);
L141:       return;
L142:     }
L143:     setResolution(resolution);
L144:   }
L145: 
L146:   function chooseCandidate(midi: number, display: string) {
L147:     const candidate = resolution.candidates.find(item => item.midi === midi && item.display === display);
L148:     if (!candidate) return;
L149:     const next = selectCandidate(candidate, query, layout);
L150:     setQuery(candidate.display);
L151:     setResolution(next);
L152:     bringIntoView(candidate.midi);
L153:   }
L154: 
L155:   function changeLayout(layoutID: string) {
L156:     audio.cancel();
L157:     const nextLayout = layouts.find(item => item.layout_id === layoutID) ?? layouts[0];
L158:     onLayoutChange(nextLayout.layout_id);
L159:     const next = resolveLookup(query || resolution.selected?.requestedSpelling.display || 'C4', nextLayout, resolution.selected?.source === 'text_query' ? 'text_query' : 'share_restore');
L160:     setResolution(next);
L161:     if (next.selected) bringIntoView(next.selected.midi, nextLayout);
L162:   }
L163: 
L164:   async function copyNoteLink() {
L165:     if (!selected) return;
L166:     const resolution = selectPianoKey(selected, layout);
L167:     const url = buildShareURL('/keyboard-notes', lookupShareParams(layout, resolution));
L168:     setCopyMessage(await copyShareURL(url) ? 'Link copied.' : 'Copy failed. Copy the address from your browser.');
```
```text
L179:       <div className="kn-v2-note-copy">
L180:         <h3>{selected?.midi === 60 ? 'Middle C' : selected ? primaryLabel(selected) : 'Choose a note'}</h3>
L181:         <p>{noteDescription(selected)}</p>
L182:         <button className="am-button am-primary kn-v2-hear" type="button" onClick={() => selected && audio.play(selected.midi)} disabled={!audio.ready || !selected}>
L183:           <Icon name={audio.state === 'playing' ? 'stop' : 'play'}/><RollingText>{audio.state === 'playing' ? 'Playing' : selected ? `Hear ${primaryLabel(selected)}` : 'Hear note'}</RollingText>
L184:         </button>
L185:         <p className="kn-v2-audio-status" role="status">{audio.message}</p>
```
```text
L361:   function practiceShareURL() {
L362:     const preset = roundPreset ?? createPracticePreset(option);
L363:     const params = practicePresetParams(preset);
L364:     return buildShareURL('/keyboard-notes', params) + '#note-trainer';
L365:   }
L366: 
L367:   async function copyPracticeLink() {
L368:     const url = practiceShareURL();
L369:     const copied = await copyShareURL(url);
L370:     setManualShareURL(copied ? '' : url);
L371:     setShareStatus(copied ? 'Link copied.' : 'Copy is unavailable. Select and copy the link below.');
L372:     emitKeyboardPracticeEvent('keyboard_practice_share_copy', { status: copied ? 'copied' : 'unavailable' });
L373:   }
L374: 
L375:   async function moreShareOptions() {
L376:     const status = await openNativeShare('Piano note practice', practiceShareURL());
L377:     if (status === 'opened' || status === 'cancelled') setShareStatus('');
L378:     if (status === 'unavailable') setShareStatus('Sharing is unavailable here. You can copy the link instead.');
L379:     emitKeyboardPracticeEvent('keyboard_practice_share_native', { status });
L380:   }
```
```text
L503:       <dialog ref={shareDialog} className="kn-v2-share-dialog" onClose={() => shareTrigger.current?.focus()}>
L504:         <div className="am-dialog-head"><div><p className="kn-v2-kicker">Share practice</p><h2>Invite someone to try it.</h2></div><button className="am-button am-tertiary" type="button" aria-label="Close" onClick={() => shareDialog.current?.close()}><Icon name="close"/></button></div>
L505:         <p>The link opens a fresh 10-note round. Your answers and score stay private.</p>
L506:         <div className="kn-v2-share-preview"><span>{optionConfig.label}</span><span>{PRACTICE_LENGTH} notes</span></div>
L507:         <button className="am-button am-primary" type="button" onClick={copyPracticeLink}><RollingText>Copy practice link</RollingText></button>
L508:         {canShare && <button className="am-button am-secondary" type="button" onClick={moreShareOptions}><RollingText>More sharing options</RollingText></button>}
L509:         <p className="kn-v2-copy-status" role="status">{shareStatus}</p>
L510:         {manualShareURL && <label className="kn-v2-manual-link">Practice link<input value={manualShareURL} readOnly onFocus={event => event.currentTarget.select()}/></label>}
```

## F05

来源：`src/components/keyboard-notes/share-control.tsx`

```text
L4: export function buildShareURL(path: string, params: URLSearchParams) {
L5:   return `${window.location.origin}${path}${params.size ? `?${params}` : ''}`;
L6: }
L7: 
L8: export async function copyShareURL(url: string) {
L9:   try {
L10:     await navigator.clipboard.writeText(url);
L11:     return true;
L12:   } catch {
L13:     return false;
L14:   }
L15: }
L16: 
L17: export async function openNativeShare(title: string, url: string) {
L18:   if (!navigator.share) return 'unavailable' as const;
L19:   try {
L20:     await navigator.share({ title, url });
L21:     return 'opened' as const;
L22:   } catch (error) {
L23:     if ((error as DOMException).name === 'AbortError') return 'cancelled' as const;
L24:     return 'unavailable' as const;
L25:   }
L26: }
L27: 
L28: export function ShareControl({ path, params, label = 'Share this note' }: { path: string; params: URLSearchParams; label?: string }) {
L29:   const [message, setMessage] = useState('');
L30:   function url() { return buildShareURL(path, params); }
L31:   async function copy() {
L32:     setMessage(await copyShareURL(url()) ? 'Link copied. It will reopen this page with the same state.' : 'Copy failed. Copy the address from your browser.');
L33:   }
L34:   async function share() {
L35:     const result = await openNativeShare(document.title, url());
L36:     if (result === 'opened') { setMessage('Sharing options opened.'); return; }
L37:     if (result === 'cancelled') return;
L38:     await copy();
L39:   }
L40:   return <div className="kn-share"><div className="kn-actions"><button type="button" className="am-button am-secondary" onClick={share}>{label}</button><button type="button" className="am-button am-tertiary" onClick={copy}>Copy link</button></div><p className="kn-status" role="status">{message}</p></div>;
```

## F06 / F09 shared skin

来源：`src/components/keyboard-notes/keyboard-notes-v2.css`

```text
L14: .kn-v2-panel{min-width:0;padding-block:clamp(2.5rem,5vw,4.5rem)}
L15: .kn-v2-kicker{margin:0 0 .5rem;color:var(--muted-foreground);font-size:var(--pr-type-meta-size);font-weight:650;letter-spacing:.045em;text-transform:uppercase}
```
```text
L35: .kn-v2-keyboard-stage .kn-key-scroll{overflow:hidden;padding:0;border-radius:0;background:transparent}
L36: .kn-v2-keyboard-stage .kn-keyboard{height:13.5rem;margin:0}
L37: .kn-v2-keyboard-stage>.kn-key-scroll>.kn-keyboard,.kn-v2-start-keyboard>.kn-key-scroll>.kn-keyboard{border-top:.625rem solid var(--piano-key-black)}
L38: .kn-v2-keyboard-stage .kn-key-text{bottom:2.1rem;font-weight:600}
L39: .kn-v2-keyboard-stage .kn-black .kn-key-text{top:.7rem;bottom:auto;font-size:.75rem}
L40: .kn-v2-keyboard-stage .kn-middle{display:none}
L41: .kn-v2-keyboard-stage .kn-state-label{position:absolute;right:.25rem;bottom:2.25rem;left:.25rem;font-size:.6875rem;font-weight:650;line-height:1.15;text-align:center}
L42: .kn-v2-keyboard-stage .kn-selected-mark{bottom:.55rem;font-size:1rem;font-weight:800}
L43: .kn-v2-keyboard-stage .kn-selected-wrong{background:#fff7f5!important;color:var(--error)!important;box-shadow:inset 0 0 0 3px var(--error)!important}
L44: .kn-v2-keyboard-stage .kn-selected-correct{background:var(--piano-note-selected)!important;color:var(--primary)!important;box-shadow:inset 0 0 0 3px var(--primary)!important}
L45: .kn-v2-keyboard-stage .kn-selected-revealed{background:var(--background)!important;color:var(--primary)!important;box-shadow:inset 0 0 0 3px var(--primary)!important;border-style:dashed}
```
```text
L85: .kn-v2-practice{max-width:64rem;margin-inline:auto;padding-top:2rem}
L86: .kn-v2-practice-start{padding-block:.75rem;text-align:left}
L87: .kn-v2-start-meta,.kn-v2-results-meta{display:flex;align-items:center;justify-content:space-between;gap:1rem;margin-bottom:1.5rem;color:var(--muted-foreground);font-size:var(--pr-type-meta-size);text-align:left}
L88: .kn-v2-start-intro{display:grid;grid-template-columns:minmax(0,1.25fr) minmax(18rem,.75fr);align-items:start;gap:clamp(2rem,5vw,4.5rem);padding-bottom:1.75rem;border-bottom:1px solid var(--border)}
L89: .kn-v2-start-copy h2{max-width:38rem;margin:0;font-size:clamp(2.15rem,4vw,3.25rem);line-height:1.04;letter-spacing:-.045em}
L90: .kn-v2-start-copy>p:not(.kn-v2-start-note,.kn-v2-start-summary){max-width:34rem;margin-top:1rem;color:var(--muted-foreground);font-size:1.05rem;line-height:1.55}
L91: .kn-v2-start-note{margin:0 0 .65rem;color:var(--primary);font-size:var(--pr-type-meta-size);font-weight:700;letter-spacing:.025em;text-transform:uppercase}
L92: .kn-v2-start-button{margin-top:1.5rem}
L93: .kn-v2-start-summary{margin-top:.8rem;color:var(--muted-foreground);font-size:var(--pr-type-meta-size)}
L94: .kn-v2-round-outcome{padding-left:1.75rem;border-left:1px solid var(--border)}
L95: .kn-v2-round-outcome ol{display:grid;gap:1rem;margin:1rem 0 0;padding:0;counter-reset:practice-step;list-style:none}
L96: .kn-v2-round-outcome li{position:relative;padding-left:2rem;counter-increment:practice-step}
L97: .kn-v2-round-outcome li::before{content:counter(practice-step);position:absolute;top:.05rem;left:0;width:1.3rem;height:1.3rem;border:1px solid var(--input);border-radius:50%;color:var(--muted-foreground);font-size:.7rem;font-weight:700;line-height:1.2rem;text-align:center}
L98: .kn-v2-round-outcome strong,.kn-v2-round-outcome span{display:block}
L99: .kn-v2-round-outcome strong{font-size:.9375rem}
L100: .kn-v2-round-outcome span{margin-top:.2rem;color:var(--muted-foreground);font-size:.8125rem;line-height:1.45}
L101: .kn-v2-start-keyboard{max-width:55rem;margin:1.5rem auto 0;pointer-events:none}
L102: .kn-v2-start-keyboard .kn-key-scroll{overflow:hidden;padding:0;border-radius:0}
L103: .kn-v2-start-keyboard .kn-keyboard{height:9.25rem}
L104: .kn-v2-start-keyboard .kn-selected{background:var(--background)!important;color:var(--foreground)!important;box-shadow:none!important}
L105: .kn-v2-start-keyboard .kn-selected-mark{display:none}
L106: .kn-v2-start-keyboard-meta,.kn-v2-question-keyboard-meta{display:flex;align-items:center;justify-content:space-between;gap:1rem;margin-top:.6rem;color:var(--muted-foreground);font-size:var(--pr-type-meta-size);text-align:left}
L107: .kn-v2-options{max-width:42rem;margin:1.25rem auto 0;border-top:1px solid var(--border);border-bottom:1px solid var(--border);text-align:left}
L108: .kn-v2-option-fields{display:grid;gap:1rem;padding:0 0 1.25rem}
L109: .kn-v2-option-fields>label{display:grid;gap:.5rem;color:var(--muted-foreground);font-size:var(--pr-type-meta-size);font-weight:650}
L110: .kn-v2-option-fields .kn-v2-checkbox{display:flex;align-items:center;gap:.75rem;color:var(--foreground);font-size:.9375rem;font-weight:500}
L111: .kn-v2-checkbox input{width:1.25rem;height:1.25rem;accent-color:var(--primary)}
```

## F08 / F10

来源：`scripts/check-hear-the-difference-browser.mjs`

```text
L49:   await context.addInitScript(() => {
L50:     Object.defineProperty(navigator, 'clipboard', {
L51:       configurable: true,
L52:       value: { writeText: async value => { window.__hearCopied = value; } },
L53:     });
L54:     Object.defineProperty(navigator, 'share', {
L55:       configurable: true,
L56:       value: async value => { window.__hearShared = value; },
L57:     });
```
```text
L63:   await page.goto(`${base}/tools/hear-the-difference`);
L64:   await page.getByRole('heading', { name: 'Hear the Difference Between Major and Minor Chords' }).waitFor();
L65:   check(`${device} no autoplay on landing`, await page.locator('.hd-page').getAttribute('data-phase') === 'landing');
L66:   check(`${device} answers disabled before comparison`, await page.getByRole('button', { name: 'Middle note' }).isDisabled());
L67:   check(`${device} helper asks to listen first`, await page.getByText('Listen to the comparison first.', { exact: true }).isVisible());
L68:   check(`${device} no horizontal overflow`, await page.evaluate(() => document.documentElement.scrollWidth <= innerWidth + 1));
L69:   if (states.includes('landing')) await screenshot(page, device, 'landing');
L70: 
L71:   await playComparison(page);
L72:   check(`${device} answers enabled after comparison`, await page.getByRole('button', { name: 'Middle note' }).isEnabled());
L73:   check(`${device} replay label after comparison`, await page.getByRole('button', { name: 'Replay comparison' }).isVisible());
L74:   if (states.includes('ready')) await screenshot(page, device, 'ready');
L75: 
L76:   await page.getByRole('button', { name: 'Low note' }).click();
L77:   check(`${device} wrong feedback does not say middle`, !/middle note|outer notes stay/i.test(await page.locator('.hd-feedback').innerText()));
L78:   check(`${device} wrong actions present`, await page.getByRole('button', { name: 'Hint' }).isVisible() && await page.getByRole('button', { name: 'Show answer' }).isVisible());
L79:   check(`${device} no reveal keyboard before reveal`, await page.locator('.hd-reveal').count() === 0);
L80:   if (states.includes('wrong')) await screenshot(page, device, 'wrong');
L81: 
L82:   await page.getByRole('button', { name: 'Hint' }).click();
L83:   check(`${device} hint copy`, await page.locator('.hd-feedback').innerText().then(text => /Listen closely to the center voice in this root-position shape/.test(text)));
L84:   check(`${device} hint still hides reveal`, await page.locator('.hd-reveal').count() === 0);
L85:   if (states.includes('hint')) await screenshot(page, device, 'hint');
L86: 
L87:   await page.getByRole('button', { name: 'Show answer' }).click();
L88:   await page.locator('.hd-reveal').waitFor();
L89:   check(`${device} show-answer eyebrow`, await page.getByText('Answer revealed', { exact: true }).isVisible());
L90:   check(`${device} reveal explains third`, await page.getByRole('heading', { name: 'The third moved up by one semitone.' }).isVisible());
L91:   check(`${device} try another is primary after reveal`, await page.locator('.hd-reveal-actions .am-primary').innerText().then(text => /Try another pair/.test(text)));
```
```text
L94:   await page.getByRole('button', { name: 'Share challenge' }).click();
L95:   await page.getByRole('dialog', { name: 'Share the listening challenge' }).waitFor();
L96:   await page.getByRole('button', { name: 'Copy challenge link' }).click();
L97:   const copied = await page.evaluate(() => window.__hearCopied || '');
L98:   check(`${device} copy link restores pair without answer`, /pair=a/.test(copied) && /from=share/.test(copied) && !/answer|middle|guess/.test(copied));
L99:   await page.getByRole('button', { name: 'Share', exact: true }).click();
L100:   const shared = await page.evaluate(() => window.__hearShared || null);
L101:   check(`${device} native share payload non-spoiler`, shared && shared.title.includes('one note') && !/C♯|middle note|C4 →/i.test(JSON.stringify(shared)));
```
```text
L121:   // Audio error surface via unavailable path is covered separately if needed; force visible panel for screenshot parity on desktop.
L122:   if (states.includes('audio-error')) {
L123:     await page.evaluate(() => {
L124:       const panel = document.querySelector('.hd-audio-error');
L125:       if (panel) return;
L126:       const stage = document.querySelector('.hd-play-stage');
L127:       const div = document.createElement('div');
L128:       div.className = 'hd-audio-error';
L129:       div.setAttribute('role', 'alert');
L130:       div.innerHTML = '<strong>Audio isn’t available right now.</strong><p>Try again or continue with the chord references below.</p><button type="button" class="am-button am-secondary">Retry audio</button>';
L131:       stage?.insertAdjacentElement('afterend', div);
L132:     });
L133:     await screenshot(page, device, 'audio-error');
L134:   }
```

## F08 固定答案

来源：`scripts/check-hear-the-difference.mjs`

```text
L38: test('four curated pairs minor→major', () => {
L39:   const pairs = getHearPairs();
L40:   assert.equal(pairs.length, 4);
L41:   assert.deepEqual(pairs.map(pair => pair.id), ['a', 'c', 'd', 'e']);
L42:   for (const pair of pairs) {
L43:     assert.equal(pair.direction, 'raise-one-semitone');
L44:     assert.equal(pair.voicing, 'root-position-close');
L45:     assert.equal(pair.answer, 'middle');
L46:     assert.equal(pair.majorNotes[1].midi - pair.minorNotes[1].midi, 1);
L47:     assert.equal(pair.minorNotes[0].midi, pair.majorNotes[0].midi);
L48:     assert.equal(pair.minorNotes[2].midi, pair.majorNotes[2].midi);
L49:   }
L50: });
L51: 
L52: test('exact B04 listening pitches', () => {
L53:   assert.deepEqual(getHearPair('a').minorNotes.map(n => n.midi), [57, 60, 64]);
L54:   assert.deepEqual(getHearPair('a').majorNotes.map(n => n.midi), [57, 61, 64]);
L55:   assert.deepEqual(getHearPair('c').minorNotes.map(n => n.midi), [60, 63, 67]);
L56:   assert.deepEqual(getHearPair('c').majorNotes.map(n => n.midi), [60, 64, 67]);
L57:   assert.deepEqual(getHearPair('d').minorNotes.map(n => n.midi), [62, 65, 69]);
L58:   assert.deepEqual(getHearPair('d').majorNotes.map(n => n.midi), [62, 66, 69]);
L59:   assert.deepEqual(getHearPair('e').minorNotes.map(n => n.midi), [64, 67, 71]);
L60:   assert.deepEqual(getHearPair('e').majorNotes.map(n => n.midi), [64, 68, 71]);
```

## F11

来源：`scripts/check-keyboard-practice.mjs`

```text
L192:   const shared = await browser.newPage({ viewport: { width: 390, height: 844 } });
L193:   shared.on('pageerror', error => runtimeErrors.push(`shared-owner: ${error.message}`));
L194:   for (const route of ['/keyboard-notes/chart', '/keyboard-notes/labeled', '/keyboard-notes/frequencies', '/keyboard-notes/blank', '/keyboard-notes/finger-numbers']) {
L195:     const response = await shared.goto(`${base}${route}`);
L196:     check(`${route} shared owner route responds`, response?.status() === 200);
L197:     check(`${route} shared owner has no horizontal overflow`, await shared.evaluate(() => document.documentElement.scrollWidth <= innerWidth));
L198:     check(`${route} canonical unchanged`, await shared.locator('link[rel="canonical"]').getAttribute('href') === `https://pianogrid.com${route}`);
L199:   }
L200:   await shared.goto(`${base}/keyboard-notes/chart`);
L201:   await shared.getByLabel('Displayed range').selectOption('61_keys');
L202:   check('chart still renders 61 shared keyboard keys', await shared.locator('.kn-screen .kn-keyboard button').count() === 61);
L203:   await shared.locator('.kn-screen button.kn-key[data-midi="60"]').click();
L204:   check('chart shared keyboard still selects exact MIDI', await shared.locator('.kn-result').getAttribute('data-selected-midi') === '60');
L205:   await shared.goto(`${base}/keyboard-notes/labeled`);
L206:   await shared.getByLabel('Keyboard layout').selectOption('61-key-C2-C7');
L207:   check('labeled route still renders 61-key layout', await shared.locator('.kn-screen .kn-key').count() === 61);
L208:   await shared.goto(`${base}/keyboard-notes/frequencies`);
L209:   await shared.getByLabel('Find by note or MIDI').fill('C4');
L210:   await shared.getByRole('button', { name: 'Find', exact: true }).click();
L211:   check('frequency owner still resolves C4', (await shared.locator('.kn-result').innerText()).includes('261.63 Hz'));
L212:   for (const route of ['/chords/a-minor', '/scales/c-major']) check(`${route} unaffected smoke`, (await shared.request.get(`${base}${route}`)).status() === 200);
L213:   await shared.goto(`${base}/keyboard-notes?${presetQuery}`);
L214:   check('shared preset keeps base canonical', await shared.locator('link[rel="canonical"]').getAttribute('href') === 'https://pianogrid.com/keyboard-notes');
```
