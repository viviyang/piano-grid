# MANUAL_CHECKS

Remaining human-only gates before any production claim:

1. Named design review of B04 + Keyboard screenshots vs approved v2 comps (1440 / 390).
2. Real device: iOS Safari + Android Chrome listen path (audio output, resume after background).
3. Screen reader pass on Practice exit dialog, Share dialog, Labeled Print/More.
4. Physical print of Labeled Letter landscape (88 then 61 / octaves off) via button and Ctrl/Cmd+P.
5. Reduced motion: no unexpected animation dependence.
6. Post-deploy observation plan only after explicit deploy authorization (not this RC).
7. PDF accessibility decision remains outside RC (unchanged).

Local preview used for agent evidence: `http://127.0.0.1:3115`  
Public site unchanged: `https://pianogrid.com`
