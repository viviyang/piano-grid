# PianoGrid｜B05 → B06 → B07 完整执行包 v2

日期：2026-09-16。用途：Cursor Auto 在最新已发布 B04 基础上实施；不是再次执行 B04/RC。

## 现在的结论
[已核实｜用户反馈 P01] B04 已提交发布。
[推断｜本包决策] 可以开始 B05；按 B05、B06、B07 串行修改同一集成仓库。每批一个新的 Cursor 实施会话；一个会话只读取当批材料。自动验收和交接完成即可交下一批，最后做一次真机验收和发布授权。不要同时开三个写代码的 Agent。

## 三批交付
| 批次 | 成品 | 不做 |
|---|---|---|
| B05 | 现有三个起步版本的清晰入口；Twinkle 确切版本的五步练习计划；Songs/Sheet 互链；完整英文内容 | 不扩曲库、不复制第三方曲谱、不假造站内播放 |
| B06 | C4–C5 三页教师资料（参考/练习/答案）；Letter/A4；页面预览、打印、下载、分享及匹配练习入口 | 不建教师账号、独立资源站或第二套键盘引擎 |
| B07 | 既有页面意图、TD/H、来源文案、内链、SEO与事件收集的逐页收口 | 不批量加URL、不做虚构KD/流量、不换主题 |

## 给 Cursor 的位置
把本文件夹内容放到 `docs/product-upgrade/b05-b07-v2/`。
不要把新旧包混放同一个目录；旧 RC 包留作历史，不作为本轮执行指令。

## 三次启动
1. 新 Cursor 会话：发送 `01_MASTER_CURSOR_PROMPT.md`，CURRENT_BATCH=B05。完成 B05 自动验收及交接后停止。
2. 新 Cursor 会话：读取 `B06/04_CURSOR_PROMPT.md`，在 B05 最新成果上做 B06。
3. 新 Cursor 会话：读取 `B07/05_CURSOR_PROMPT.md`，在 B05+B06 最新成果上做 B07。

中途只处理真正阻塞：仓库不可访问、权限、版本权利缺证或共享回归失败。已有产品/设计决策不要重新询问。
最终不自动 commit/push/deploy、不发外联邮件、不接入未经授权的统计服务。

## 用户看什么
- `00_design_overview.html`：一次浏览设计、26张截图、PDF和实施文件。
- `design/index.html`：能点击的 Songs/五步计划/曲谱访问/教师资料设计参照。
- `design/screens/`：同一 HTML 实际渲染的桌面和手机状态。
- `print/`：Letter、A4 三页纸面样张。它们是内容和排版参照；生产 PDF 要从项目共享键盘数据生成，不能形成第二套数据。
- `B07/02_PAGE_TDH_MAP.md`：逐页 Title、Description、H1–H3。

## 本包承诺与边界
[已核实｜本次制作] 包内给出可执行规格、英文文案、结构化内容、链接清单、交互原型、纸面样张、任务与验收台账。
本包不是你的仓库补丁。所有生产验收初始为 PENDING；原型自测不等于网站测试通过。
[已核实｜公开观察 W01–W08] 三个起步版本及其外部访问方式已有公开依据。本次未读取最新本地 Songs registry/主题配置，Cursor 必须核对真实 owner、ID 与 token。
不能因浏览抓取缓存未显示某项就回滚 B04。B04 页面本次一次读取失败，不据此认定发布失败；以实际 HTTP、仓库与发布记录复核。

## 可选Cursor规则文件
本包内`.cursor/rules/pianogrid-b05-b07.mdc`是备用规则，不会因为放在docs子目录就自动成为项目规则。主Prompt已经包含必需约束，因此不依赖它。需要启用时合并到仓库根`.cursor/rules/`并按Cursor实际规则机制引用，保留原有规则，不覆盖AGENTS.md。
