# Handoff — B06

Recoverable notes for the next session. Do **not** reset the dirty worktree.

## New production files
See `NEW_FILES.txt`.

## Critical fix learned
Teaching pack print DOM must not nest under `.kn-screen`; labeled print CSS sets `.kn-page .kn-screen { display:none }`.

## Dev server
Next 16 blocks HMR from `127.0.0.1` when Local is `localhost`. Browser/PDF scripts must use `http://localhost:3116`.
