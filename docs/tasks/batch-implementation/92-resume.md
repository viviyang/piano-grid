# 92 — 中断或新对话恢复

这是既有Piano Reference任务的恢复，不是新项目或新设计。先读MASTER-RULES、AGENTS、docs/tasks/site-implementation-plan.md、当前批次implementation/review/fixes及实际git状态。

若是主开发角色：
- 从已授权但尚未完成的具体检查点继续。
- 只有等待修复时执行91，只有用户指定下一批时才放开该批路由。
- 旧开发对话停止写入，不同时两个开发者改同一checkout。
- 不把新对话当成依赖重装、Foundation重做或127页全开授权。

若是验收角色：
- 只读产品源码，沿用90的边界。
- 复验当前代码/哈希，报告历史与本次证据分开。

先用几行说明：发现的实际进度、当前授权、剩余阻塞、接下来动作，然后立即继续已授权工作；不是只输出新的计划。
当上下文不足时先查仓库文件，不要求用户粘贴几百条聊天；文件中确实缺关键决策时只问那一项。
