# 01 — 和弦页面组

先读 `MASTER-RULES.md`，确认00接入已完成或运行其必要步骤。
任务ID：`01-chords`。

## 仅授权

新增 `/chords`（T06）、`/chords/a-major`（T07）、`/chords/c-major`（T07）。
`/chords/a-minor` 保持既有成品，作为T07和共享组件回归基线。
只实现这些路由和必要基础组件，不暴露master中的其他和弦详情/反查/走向路由。

## 精确内容来源

- `/chords`：总包中原样保留的旧对象及legacy_chords_support。
- `/chords/a-major`、`/chords/c-major`：总包pages中的新对象，说明来源目录为F-Homepage。
- 旧页资产以preserved-chords为根，新详情以总包为根。

不能仅因为F批次已有8个新和弦对象，就擅自把旧中心“9个命名和弦”的英文正文/PDF改成更大集合；以该页最新被明确指定的内容为准。目录F不意味着这些内容必须等首页开发。

## 实现

1. 抽取已验收T07实现为必要的可复用模板（如已经复用，不再重构）。读取每页自己的完整数据、正文、问题和PDF，不用字符串替换A minor名称生成假内容。
2. A major/C major逐个接入其真实位置、播放、当前打印、静态PDF和解释。指法只显示满足该页审核条件的公开字段。
3. 中心默认直接展示实际集合，不是空筛选器；根音/类型只开放已有集合；每个结果直接给音名、键盘和试听，详情链接不是完成查询的必经步骤。
4. 根音/类型筛选、清除筛选、零结果状态、当前结果齐奏/逐音/停止、当前打印/筛选集合打印按旧页原契约接入。过滤掉当前播放对象时取消旧声音；空结果禁用相应动作。
5. 保留major/minor两组对比的具体图/数据与解释；不能用筛选按钮代替内容章节。
6. 未准备的power、7th、jazz/full/all任务继续记录，不显示假按钮、不添加未提供的集合。
7. 多结果不同时独立持续播放；复用同一受控声音机制或可靠的互斥停止机制，不新增播放器侧栏。
8. 新增3页元数据使用各页原内容，noindex保持。现有链接只按本地预览/正式发布分离规则处理。

## 验收

新增3 URL逐页检查，A minor三转位完整回归。
重点：黑键C-sharp位置与标记；B/C-flat相同物理键不改拼写；旧集合9个名称不冒称9种不同键位；类型/根音组合确实返回预期对象；空结果不残留声音；当前与筛选打印正确。

1440/390真实截图，320/768布局检查；完整英文模块和逐source_group required_output映射；PDF实际打开核对。
跑check、相关测试、build。输出 `checks/batches/01-chords/implementation.md` 及进度表更新。
完成后停止供独立验收，不自动进入02。普通组件实现过程无需逐项问用户。
