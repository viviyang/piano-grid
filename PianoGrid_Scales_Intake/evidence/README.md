# Evidence index

## current-public

[已核实] 来自 `checks/release/public-launch/independent-reverification/`：三页 1440/390 完整截图、原始 HTML、响应头、生产验证、发布边界和独立报告。报告日期为 2026-09-10，三页线上技术状态为 PASS；仍保留真机、读屏、原生/实体打印和真人听音门槛。

## current-local

[已核实] `computed-styles.json` 记录 2026-09-11 在现有 Chrome 中逐页打开 `127.0.0.1:3000` 后的默认值、章节、控件启用计数与主要计算样式。localhost 仅是用户机器页面，不是 ChatGPT 以后可访问的链接。

## existing-scale-review

[已核实] 2026-09-09 的 Scales 独立验收报告、汇总、机器结果与四张打印渲染图；结论 `PASS_WITH_NOTES`。这些是原有证据，未在本轮重跑。

## chords-b3

[已核实] 仅保存 B3 最新状态与 adapter 验证结果，用来说明共享组件当前背景；没有重新验收 Chords 四页。

## workspace-status.txt

[已核实] 生成时 HEAD、分支与完整 dirty 清单。它用于辨认并行变化，不表示包修改了这些业务文件。

